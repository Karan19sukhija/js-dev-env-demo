/*

Want to run this file before the server runs. So we will use prestart script in package.json for this

*/

//var chalk = require ('chalk');
import chalk from 'chalk'; // written in ES6 format. Bable node transpiles to ES5 and send to noe.js

console.log(chalk.green('Starting app in Dev mode...')); // eslint-disable-line no-console

